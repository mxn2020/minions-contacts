[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "minions-contacts"
version = "0.2.0"
description = "People, organizations, and contact channels"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
keywords = ["contacts","ai","minions"]
authors = [
    { name = "Mehdi Nabhani", email = "mehdi@the-mehdi.com" }
]
dependencies = [
    "minions-sdk>=0.2.1",
]

[project.optional-dependencies]
test = ["pytest>=7.0"]

[project.urls]
Homepage = "https://github.com/mxn2020/minions-contacts"
Repository = "https://github.com/mxn2020/minions-contacts"

[tool.hatch.build.targets.wheel]
packages = ["minions_contacts"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
